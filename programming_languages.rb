def reformat_languages(languages)
  new_hash = {}
  languages.each do |type, languages|
    # if language[:type] == "interpreted"
    #   language[:style] = [:oo]
    #   new_hash = language
    # end 
  end 
  
  return new_hash
end
